{
  "name": "@job-portal/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared utility functions for the Job Portal platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "@job-portal/config": "workspace:*",
    "typescript": "^5.4.5"
  }
}
